apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: production-itsm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ay0unx/itsm-backend:latest
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: JWT_SECRET
            - name: JWT_REFRESH_SECRET
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: JWT_REFRESH_SECRET
            - name: WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: WEBHOOK_SECRET
            - name: POSE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: POSE_API_TOKEN
            - name: PRITUNL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: PRITUNL_USERNAME
            - name: PRITUNL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: itsm-secrets
                  key: PRITUNL_PASSWORD
          envFrom:
            - configMapRef:
                name: itsm-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: production-itsm
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
