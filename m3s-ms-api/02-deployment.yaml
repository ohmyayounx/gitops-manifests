apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-api
  namespace: m3s-ms-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-api
  template:
    metadata:
      labels:
        app: ms-api
        project: m3s
    spec:
      containers:
        - name: ms-api
          image: devopspcs/ms-api-go:latest  # Tag akan di-update oleh Jenkins via Git commit
          ports:
            - containerPort: 8002
          envFrom:
            - secretRef:
                name: ms-api-env
          env:
            - name: GOOGLE_CREDENTIALS_FILE
              value: "/app/service_account_credential.json"
          volumeMounts:
            - name: gcp-credentials
              mountPath: /app/service_account_credential.json
              subPath: service_account_credential.json
              readOnly: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 30
      volumes:
        - name: gcp-credentials
          secret:
            secretName: ms-api-gcp-credentials
      imagePullSecrets:
        - name: dockerhub-pull
