##############################################
# Prerequisites:
# 1. Install External Secrets Operator di cluster:
#    helm repo add external-secrets https://charts.external-secrets.io
#    helm install external-secrets external-secrets/external-secrets -n external-secrets --create-namespace
#
# 2. Buat Secret untuk Vault auth (sekali saja):
#    kubectl create secret generic vault-credentials \
#      --from-literal=username=<vault-user> \
#      --from-literal=password=<vault-pass> \
#      -n m3s-ms-api
##############################################

# ClusterSecretStore - koneksi ke Vault (bisa di-share semua namespace)
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: m3s-ms-api
spec:
  provider:
    vault:
      server: "http://10.184.20.4:8200"
      path: "development"
      version: "v2"
      auth:
        userPass:
          path: userpass
          username: "devopspcs"
          secretRef:
            name: vault-credentials
            key: password
---
# ExternalSecret - sync env secrets dari Vault ke K8s Secret
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ms-api-env
  namespace: m3s-ms-api
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: ms-api-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: m3s/api/ms-api/env
---
# ExternalSecret - sync GCP service account dari Vault
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ms-api-gcp-credentials
  namespace: m3s-ms-api
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: ms-api-gcp-credentials
    creationPolicy: Owner
  data:
    - secretKey: service_account_credential.json
      remoteRef:
        key: m3s/api/ms-api/service_account_credential
        property: content
